/*
    Assign C cows to N stall that min distance between them is largest possible.
    return largest minimum distance

*/

#include <iostream>
#include <vector>
using namespace std;

int LargestMinimumDistance(vector<int>& pos, int n){
    int distance;
    return distance;
}

int main() {
    vector<int> pos = {1, 2, 8, 4, 9};
    int n = pos.size();
    cout << LargestMinimumDistance(pos, n);
    return 0;
}